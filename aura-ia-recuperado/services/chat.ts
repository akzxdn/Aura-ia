import type {Conversation,Message} from "@/lib/aura-types";
export async function checked(response:Response){if(!response.ok){if(response.status===401)throw new Error("Entre com sua conta para conversar e salvar o histórico.");let message="Não foi possível concluir. Tente novamente.";try{message=(await response.json()).error||message}catch{}throw new Error(message)}return response}
export async function saveConversation(c:Conversation){return checked(await fetch("/api/conversations",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(c)}))}
export async function streamReply(messages:Message[],signal:AbortSignal,onDelta:(value:string)=>void){const response=await checked(await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({messages}),signal}));const reader=response.body?.getReader();if(!reader)throw new Error("Resposta indisponível.");const decoder=new TextDecoder();let pending="",complete=false;
try{while(true){const {value,done}=await reader.read();if(done)break;pending+=decoder.decode(value,{stream:true});const events=pending.split(/\r?\n\r?\n/);pending=events.pop()||"";for(const event of events){for(const line of event.split(/\r?\n/)){if(!line.startsWith("data: "))continue;const raw=line.slice(6);if(raw==="[DONE]"){complete=true;continue}const data=JSON.parse(raw);if(data.type==="response.output_text.delta")onDelta(data.delta);if(data.type==="response.completed")complete=true;if(data.type==="error"||data.type==="response.failed"||data.type==="response.incomplete")throw new Error("A resposta foi interrompida. Tente novamente.")}}}if(!complete&&!signal.aborted)throw new Error("A conexão foi interrompida antes de concluir a resposta.")}finally{reader.releaseLock()}}
